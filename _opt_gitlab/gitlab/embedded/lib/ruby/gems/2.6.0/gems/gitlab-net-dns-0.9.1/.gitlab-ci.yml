image: "ruby:2.6-alpine"

cache:
  paths:
    - vendor/ruby

before_script:
  - apk add --update git make build-base
  - gem install bundler
  - bundle install --path vendor/ruby

rubocop:
  script:
    - bundle exec rubocop
  allow_failure: true

rspec:
  script:
    - bundle exec rake

